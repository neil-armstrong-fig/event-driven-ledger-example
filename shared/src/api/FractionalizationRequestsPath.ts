/** The API Gateway resource path fractionalization requests are POSTed to — served by `infra`, called by the acceptance tests. */
export const FRACTIONALIZATION_REQUESTS_PATH = "fractionalization-requests";
